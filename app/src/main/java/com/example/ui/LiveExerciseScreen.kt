package com.example.ui

// Camera + MediaPipe live verification is intentionally removed from the V1 build.
// The original generated file had invalid Kotlin imports and unverified API calls
// that broke compilation. The tested exercise-verification engine remains in
// com.example.exercise; wire a real camera screen on-device later.
